import 'dart:ui';



class WindowConstants {
  // A/B 页面的最小尺寸
  static const Size abMinSize = Size(400, 850);
  // C 页面的目标尺寸及最小尺寸
  static const Size cTargetSize = Size(100, 300);
  static const Size cMinSize = Size(100, 300);
}